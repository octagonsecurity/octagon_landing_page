Uppercase, wide-tracked red kicker with a leading rule — the brand section eyebrow.

```jsx
<Eyebrow>Why Choose Octagon</Eyebrow>
<Eyebrow rule={false} inverse>Our Services</Eyebrow>
```

Props: `rule` (leading red dash, default true), `inverse` (white, for dark bands).
