Section header lockup — eyebrow + bold title + red accent rule + subtitle.

```jsx
<SectionTitle
  eyebrow="Why Choose Octagon"
  title="Your Partner in Complete Property Protection"
  subtitle="Over 65 years of combined experience across South Florida."
/>
<SectionTitle title="Our Services" align="center" />
```

Props: `eyebrow`, `title`, `subtitle`, `align` (left/center), `inverse` (dark bg), `rule`.
